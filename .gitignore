# Sistema operativo
.DS_Store
Thumbs.db
desktop.ini

# Editores
.vscode/
.idea/
*.swp

# Variables de entorno (nunca subir secretos)
.env
.env.local
.env.*.local

# Dependencias / builds (por si más adelante sumás tooling)
node_modules/
dist/
.cache/

# Netlify
.netlify/

# Claude Code (config local)
.claude/

# Temporales de generación de PDFs
.tmp-pdf/

# Archivos de trabajo locales que NO deben publicarse en el sitio
# (el deploy.sh hace `git add -A`, así que conviene listarlos acá)
/CLAUDE.md-marianocalandra.com.md
/VIdeos - ashboard SaaS Premium para ROAS.mp4
